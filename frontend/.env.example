# URL da API do backend. Em desenvolvimento local pode ficar por definir
# (cai para http://localhost:3001/api). Em producao (Vercel), define isto
# como env var VITE_API_URL apontando para o backend deployado (ex: Render).
VITE_API_URL=http://localhost:3001/api
